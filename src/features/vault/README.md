# vault Feature

> **Status**: Scaffold only — implementation coming in a dedicated feature pass.

This directory will contain all screens, components, hooks, and types
specific to the `vault` feature area.
